using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace DownloaderTest
{
	[TestClass]
	public class UnitTest1
	{
		[TestMethod]
		public void TestIfNumbersAreEqual()
		{
			// throw Exception
			// mockHttpClient = new HttpClient(new MockMessageHandler(timeout));
			// downloader = new Download(mockHttpClient);
			// downloader.DownloadChunk
			throw new NotImplementedException();
			Assert.AreEqual(1, 0, "数字不对等");
		}
	}
}
